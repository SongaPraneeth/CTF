**Description:**

Decrypt this message.

**Steps to solve:**

step1: After downloading the file , use `cat` to read the contents of the file, the command is `cat ciphertext`

step2: And we will get the flag, but it is encrypted, as we know the it is `Caesar cipher`, so use `cyber chef` and choose `Rot 13` and rotate the until the `cipher text` become any `sensible` and that is the flag

![image alt](https://github.com/SongaPraneeth/CTF/blob/036511337f6bc812db59099a973a2d42fb34dd4f/CTFs%20Write%20ups/images/picoctf_images/Crypto/Medium/caesar_1.png)

![image alt](https://github.com/SongaPraneeth/CTF/blob/036511337f6bc812db59099a973a2d42fb34dd4f/CTFs%20Write%20ups/images/picoctf_images/Crypto/Medium/caesar_2.png)